完成各个站台的机械手的信号映射,更新各个站台机械手的同步和更新函数.

主界面第三行的样式需要调整

了解已上料量是什么

让运行时间能够正常显示

修改工站Activity的运行函数, 将 short 改为 int

料盒没有升起

机械臂到中间会抖动

小车没有回到上料位

检查每个Activity的 OnPause 和 OnResume 函数, 应该要在 onResume 继续读取

应该清理掉箱子

有时候同步会不正常

机械手在同步阶段(可能是这个阶段)可能会抖

卸料过程, 因为下料位没有挡板, 所以小车2到达下料位后动画小车才动, 此时小车2可能回钩了, 导致动画小车也回钩, 箱子没有跟着动画小车走

