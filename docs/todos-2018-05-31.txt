小车在中间的时候可能抖动, 应该像在右边的时候一样加多一个判断
-done

主界面: 服务器运行时间
-almost done
需要再确认一下读取到的数字是怎么样的, 有种好像是浮点数的感觉, 如果是的话, 目前假设读取到的是整数的做法可能有问题

单工站界面: 如何获取到工站的开机时间或者运行时间

目前，每个页面都发出自己的请求，其实可以做成的一个服务，每个页面都用这个服务读取的数据即可。

主界面的已上料量是什么？

怎么获取到速度呢？需要时间和路程
路程怎么获取？
需要一个全局变量来记录从整机开机到现在已经加工完成的数量.
如果没办法直接读取这个量，那么断线之后又该怎么办呢？
结论：如果这个量没法直接读取的话，断线重连之后，程序是无法获取到当前速度的。
有没有信号直接对应当前速度呢？


重新研究一下箱子从储料位到加工位的逻辑，毕竟那是在没有实地调试之前写的，找出明显的错误并改正。
我的担忧：
小车从加工位到储料位，此时储料位好像没有挡板升起，小车到达储料位这个信号也不会亮（好像是因为触发信号的位置还要更前一点），如果是这样的话，小车无法到达储料位，出钩也无法获取箱子。
真的发生上述情况，就需要找其他信号来判断小车到达储料位，这需要时间和观察。

